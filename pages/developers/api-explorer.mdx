import ApiExplorer from '../../components/ApiExplorer'

# API Explorer

Browse every REST endpoint interactively — request/response schemas, parameters, and
ready-to-copy examples — rendered from the gateway's OpenAPI specification
([`openapi.json`](/openapi.json), snapshotted from the live gateway).

The gateway also serves this spec live with Swagger UI at
[noetherapi-production.up.railway.app/docs](https://noetherapi-production.up.railway.app/docs).

<ApiExplorer />
